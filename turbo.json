{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "cache": false,
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "public/**", "build/**"],
      "env": [
        "BUILD_GOOGLE_CLIENT_ID",
        "BUILD_GMAIL_SECRET",
        "BUILD_GITHUB_CLIENT_ID",
        "BUILD_GITHUB_SECRET",
        "BUILD_AIRTABLE_CLIENT_ID",
        "BUILD_TRELLO_API_KEY",
        "BUILD_POSTHOG_TOKEN",
        "BUILD_POSTHOG_HOST",
        "BUILD_POSTHOG_API_HOST"
      ]
    },
    "build:frontend": {
      "cache": false,
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "build:electron": {
      "cache": false,
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false
    },
    "type-check": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test": {
      "cache": false,
      "dependsOn": ["build"]
    }
  }
}
